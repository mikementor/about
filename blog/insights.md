Some insights

```
Automated Checking is vulnerable to false positives (green(passed) when it should be red(failed))
Cause when its green we don't recheck it manually
False negatives(red(failed) when it should be green(passed)) are annoying, but visible.
```
